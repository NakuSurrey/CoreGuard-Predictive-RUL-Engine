# Models subpackage
"""
Contains all model training, evaluation, and saving logic.
XGBoost (baseline) and LSTM (advanced) live in separate modules.
The evaluate module compares both models head-to-head.
"""
